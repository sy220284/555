# AGENT.md — AI 自动开发总规则

本文件是所有编程代理、评审代理、测试代理和内容代理的强制执行规则。

## 1. 最高优先级

1. `docs/design/00_MASTER_GDD.md`：产品与玩法上位约束。
2. `docs/development/00_ARCHITECTURE.md`：代码架构上位约束。
3. `docs/specs/CONTENT_REGISTRY.md`：稳定 ID 与对象最低字段。
4. `docs/specs/*`：单位、建筑、武器、科技、经济、AI、地图、战役、网络、测试的执行级规格。
5. 如文档互相冲突：先提交文档修订，再修改代码；禁止私自选择一份忽略另一份。

## 2. 编码前强制阅读

任何任务开始前必须读取：

- 本文件；
- `docs/design/00_MASTER_GDD.md`；
- `docs/development/00_ARCHITECTURE.md`；
- `docs/specs/CONTENT_REGISTRY.md`；
- 当前任务对应的执行规格；
- `docs/specs/TEST_MATRIX.md`；
- `docs/specs/ACCEPTANCE_CRITERIA.md`。

若当前对象没有稳定 ID、具体参数、状态机/公式或验收用例，禁止直接“凭经验”编码；先补规格文档。

## 3. 工作模式

采用**全量并行、接口优先、持续集成、自动闭环**模式。

- 所有核心模块和内容 ID 从第一天建立。
- 未完成能力必须提供可运行占位实现，不得长期阻塞主线。
- 每个任务独立分支/工作树；主分支始终可编译、可启动、可运行基础对局。
- 不允许以“后续补测试”“以后再优化”“暂时写死”为完成条件。
- 对重复任务沿用已经验证的正确工作路径，不重复发散重做方案。

## 4. 模块所有权

默认目录：

- `Simulation`：确定性模拟与状态。
- `Gameplay` / `Combat`：玩法与战斗规则。
- `Robotics`：机器人、自主等级、算力、失联降级。
- `AI`：玩家 AI 指挥、电脑对手、战斗群与战区决策。
- `Navigation`：分层寻路、交通、局部避障。
- `Intel` / `EW`：情报、传感器、战争迷雾、电子战、网络战。
- `Air` / `Naval` / `Orbital`：空、海、轨道支援。
- `Network`：指令同步、校验、重连、录像。
- `Data`：单位/建筑/武器/国家/科技数据。
- `Tools` / `Editor`：编辑器、调试与分析工具。

跨模块修改先走接口变更；禁止一个代理为了方便直接重写他人模块。

## 5. 稳定 ID 与数据规则

- 代码、地图、战役、资源、测试统一引用稳定 ID，禁止把显示名称当键。
- 核心国家/单位/建筑/科技/地图/任务 ID 以 `CONTENT_REGISTRY.md` 为准。
- 内容对象最低字段必须满足注册表要求；字段不适用要显式写 `0/none/[]`。
- 禁止大量 `if (country == X)`；国家差异通过 `FactionDefinition / CountryModifier / UniqueUnits / UniqueTech / StrategicAbility` 数据化实现。
- 平衡数值以执行规格为初始基线，任何修改必须同步文档并有测试依据。

## 6. 编码原则

- C++ 核心模拟优先数据导向设计；避免每单位重对象层级和高频虚调用。
- 模拟层不得引用渲染、界面、音频对象。
- 随机必须来自确定性随机源，并显式记录种子/流。
- 核心状态不得依赖系统时间、线程时序或不确定容器遍历顺序。
- 复杂 AI 决策低频更新；单位本地控制器保持轻量。
- 所有计算公式优先实现为可测试纯函数，避免散落魔法常量。

## 7. AI 指挥红线

默认 AI 可以执行：队形、局部避障、自动反击、维修、补给、无人侦察、战斗群路线、授权战区防守/有限反击。

未经玩家明确授权，AI 不得：

- 使用核武或国家终极能力；
- 动用保留战略资源；
- 改变玩家主科技路线；
- 拆除核心基地；
- 全面撤退或改变总主攻方向；
- 自动生产超级单位。

AI 不能获得伤害、生命、射速或作弊视野加成。行为状态机、评分和难度边界以 `AI_ROBOTICS_EW_STATE_MACHINES.md` 为准。

## 8. 机器人规则

机器人必须同时受资源、电力、指挥容量与算力约束。网络中断按自主等级降级，不得“一断网全部停机”。电子战只降低能力，不做万能秒杀。人类、机器人、无人平台必须长期共存，不允许高科技阶段淘汰全部有人单位。

## 9. 地图与战役规则

- 16张地图的尺寸、资源、路线、设施和天气以 `MAP_SPECS.md` 为准。
- 24关主线的目标、触发、难度、存档点和剧情揭示以 `CAMPAIGN_MISSION_SPECS.md` 为准。
- 任务脚本禁止依赖单帧巧合；关键事件必须有状态条件与超时兜底。
- 地图进入排位前必须通过出生位与资源自动公平性测试。

## 10. 网络、存档与模组

- 网络采用确定性指令同步；不得改成逐帧同步所有实体。
- 存档/录像/模组格式必须带版本与内容哈希。
- 联网模组必须内容哈希一致；排位只加载官方模拟数据。
- 脚本运行在沙箱，禁止任意文件系统/网络/进程权限。

## 11. 完成定义

一项功能只有同时满足以下条件才可标记完成：

- 代码/数据完成；
- 单元/集成测试完成；
- `TEST_MATRIX.md` 中相关用例通过；
- 确定性门禁通过；
- 性能没有超预算；
- 网络/录像需要时可复现；
- 文档同步；
- AI 能正确使用或明确标记不可使用；
- 有明确反制与错误降级路径。

不存在“差不多完成”。

## 12. 自动修复流程

失败 → 最小复现 → 定位根因 → 最小修改 → 回归 → 确定性/性能复测。除非根因证明结构不可维护，否则禁止先重写整个模块。

修复必须新增对应回归用例；只修代码不补回归不算闭环。

## 13. 主分支门禁

主分支必须始终：

- 编译成功；
- 启动成功；
- 能加载测试地图；
- 能运行确定性模拟；
- 自动测试通过；
- 数据引用无悬空 ID；
- 基础存档/录像可用。

任何提交导致主线不可用，立即回滚并开独立修复任务。

## 14. 开发决策原则

新增单位必须回答：它解决什么问题、现有单位为什么不能替代、玩家是否能一眼理解、什么能反制它。

新增系统至少满足一项：增加战略选择、增加战斗爽感、降低微操、强化阵营特色、强化世界辨识度。否则删除。

不得自行改变：四大体系、15国世界结构、五级科技、核心资源种类、AI权限红线、轨道战略层定位、机器人与有人部队共存原则。需要改变这些内容必须先提交设计变更。

最终判断：**好玩优先于写实，清晰优先于复杂，体系优先于单项参数；实现必须服从已冻结规格，平衡必须服从测试闭环。**