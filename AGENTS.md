# AGENTS.md

## 1. 项目与权威性

本仓库是 DeepSeek Harness 的精简二次开发基线。

DeepSeek Harness 是基于 Cordis 插件体系构建的可组合智能体运行平台，主要能力包括：

- 智能体运行循环；
- 模型接入；
- 上下文与系统提示词；
- 工具注册与执行；
- 会话持久化；
- 文件系统；
- Shell 与持久终端；
- 代码执行；
- 语言服务器能力；
- 沙箱；
- 网页访问；
- MCP；
- 技能；
- 子智能体；
- 后台任务；
- 工作流；
- TypeScript / Python SDK；
- ACP；
- Web 图形界面。

本文件是仓库级 Agent 开发规则。

若以后存在子目录 `AGENTS.md`，子目录规则只能补充该目录的局部约束，不得削弱或覆盖本文件中的仓库级不变量。

## 2. 权威顺序

发生冲突时固定按以下顺序处理：

```text
用户当前最新明确指令
>
用户已经明确批准的任务范围、设计或验收要求
>
AGENTS.md
>
README.md
>
根 package.json、pnpm-workspace.yaml、tsconfig、构建与测试配置
>
当前功能包 package.json、接口定义和测试
>
现有源码实现
>
历史实现习惯
```

规则：

- 用户最新明确指令优先。
- 禁止凭已有代码反向覆盖用户明确需求。
- 禁止为了保持旧实现而曲解任务目标。
- 发现需求、测试、接口、源码互相冲突时，必须先确认真实现状和影响范围，再选择能保持整体一致性的方案。
- 禁止静默绕过冲突。
- 不得自行扩大产品范围。

## 3. Agent 工作模式

默认采用：

> 独立推进 → 完整实现 → 主动验证 → 全链路审计 → 发现问题继续修复 → 再验证 → 最终闭环。

用户下达明确开发任务后，只要任务范围可以从仓库和现有上下文中确定，Agent 应独立推进，不应把正常工程判断不断退回给用户。

允许自主决定：

- 具体代码组织；
- 局部实现方式；
- 必要测试；
- 影响范围内的关联修改；
- 明确根因后的局部重构；
- 为实现正确行为必须同步调整的内部接口。

不得自行决定：

- 改变产品定位；
- 删除已有核心能力；
- 更换核心技术路线；
- 引入新的外部服务；
- 大规模替换框架；
- 改变公开协议；
- 破坏兼容性；
- 无任务需要地新增生产依赖；
- 创建新的仓库治理体系；
- 创建未经要求的分支、PR 或自动工作流。

遇到普通实现细节时直接做出最合理决策。

只有涉及产品方向、不可逆数据变化、公开协议破坏或用户明确保留裁决权的事项，才需要停止并请求裁决。

## 4. 强制启动顺序

开始编码、修复、重构、测试或架构调整前，必须依次完成：

```text
1. 读取 AGENTS.md
2. 读取 README.md
3. 读取根 package.json 和 pnpm-workspace.yaml
4. 确认当前分支、HEAD、工作区状态
5. 定位任务涉及的 packages / apps
6. 读取对应 package.json、tsconfig 和配置
7. 阅读相关实现
8. 阅读现有相关测试
9. 搜索接口调用方、注册方和消费者
10. 建立本任务的完整影响链
11. 再开始修改
```

禁止：

- 只找到一个报错文件就直接修改；
- 只阅读测试不读实际调用链；
- 只阅读接口定义不检查实现；
- 只阅读当前包而忽略跨包依赖；
- 根据文件名猜架构。

## 5. 新任务基线复核

每个任务开始时都必须以当前真实代码为基线重新核对：

- 已有能力；
- 已有插件；
- 已有接口；
- 已有公共组件；
- 已有工具；
- 已有测试设施；
- 当前依赖关系；
- 当前错误语义；
- 当前生命周期；
- 当前持久化方式；
- 当前安全边界。

开始实现前回答三个问题：

```text
这个能力是不是已经存在？
是否存在可以复用的公共机制？
如果新建一套，会不会形成第二套真源？
```

禁止重复建设已有能力。

## 6. 核心架构不变量

### INV-001 插件优先

DeepSeek Harness 的主要能力通过 Cordis 插件组合。

新增功能首先判断：

```text
已有能力扩展
→ 已有插件扩展
→ 新插件
→ 最后才考虑修改核心运行循环
```

禁止为了局部需求把功能直接硬编码进核心循环。

### INV-002 能力定义与实现分离

公共能力应保持：

```text
能力接口
   ↓
实现提供方
   ↓
消费者
```

上层模块应依赖能力契约，而不是依赖具体实现内部状态。

本地、沙箱、远程或其他执行方式需要能够通过实现替换，而不是复制整套上层逻辑。

### INV-003 会话事件是运行事实源

会影响以下行为的信息：

- 模型上下文；
- 会话恢复；
- 会话分叉；
- 状态重建；
- 用户界面；
- 查询；
- 统计；
- 审计；

应能够从权威会话事件和持久状态中重新构建。

禁止：

- UI 内维护另一套权威业务状态；
- 缓存改变业务事实；
- 日志改变运行结果；
- 恢复后的会话与原始会话产生语义漂移。

### INV-004 执行环境可替换

文件、Shell、终端、代码执行和开发工具应尽可能通过公共执行能力工作。

必须避免：

```text
本地文件工具一套逻辑
沙箱文件工具另一套逻辑
远程文件工具第三套逻辑
```

正确方向是共享上层语义，只替换底层能力提供方。

### INV-005 工具调用必须经过统一边界

模型工具不得绕过正式工具执行链直接操作底层资源。

工具执行必须考虑：

- 输入验证；
- 权限；
- 执行环境；
- 生命周期；
- 取消；
- 超时；
- 错误传播；
- 结果大小；
- 会话记录；
- 用户可见状态。

安全和权限必须由代码保证，不能依赖模型“自觉遵守”。

### INV-006 Host 与 Client 边界明确

浏览器界面只负责交互与展示。

涉及：

- 本机文件；
- 进程；
- 环境；
- 凭据；
- 系统资源；
- 服务端状态；

的能力必须通过正式宿主能力和协议边界提供。

禁止为了方便把宿主能力直接泄漏给浏览器端代码。

### INV-007 仓库范围边界

当前仓库不包含：

```text
.agents/
docs/
website/
examples/
根 assets/
额外仓库治理体系
社区素材
外部开发规则
```

除非用户明确要求新增，否则：

- 不得因为测试方便自行恢复这些目录；
- 不得为了满足历史结构重新引入无关文件；
- 不得为了让个别测试通过引入与产品无关的资产；
- 不得无任务需要扩大仓库内容范围。

新增文件或目录前必须判断：

> 它是否真实属于产品运行、开发、测试或维护闭包？

不属于则不得加入。

## 7. 模块边界

主要工程领域：

```text
apps/
  cli
  web

packages/
  core
  llm
  context
  compaction
  fs
  shell
  subprocess
  terminal
  code-runtime
  lsp
  sandbox
  web
  mcp
  skill
  subagent
  workflow
  jobs
  session
  session-query
  storage
  interaction
  plan
  goal
  todo
  schedule
  api
  sdk
  acp
  client
  host
  bundle
  preset
  extensions
  hooks
  util
  test-support
  ...

native/
python/
vendor/
scripts/
patches/
```

修改任何模块时都要检查其：

```text
定义方
→ 注册方
→ 调用方
→ 状态持有方
→ 持久化
→ UI消费者
→ 测试
```

禁止仅从当前目录判断影响范围。

## 8. 完整调用链审查

修改前和修改后都必须检查横向与纵向影响。

### 横向

至少包括：

- 同类工具；
- 同类插件；
- 共享服务；
- 共用契约；
- 共用状态；
- 公共 UI；
- 公共测试设施。

### 纵向

视功能实际链路检查，例如：

```text
Client
→ API
→ Host
→ Agent
→ Tool
→ Capability
→ Runtime
→ Persistence
```

或：

```text
用户输入
→ Session
→ Context
→ LLM
→ Tool Call
→ Tool Execution
→ Session Event
→ Client Projection
```

不能只验证链路中间的一层。

## 9. 根因治理原则

修复缺陷时必须先定位：

> 最早在哪一层失去了正确约束？

需要区分：

```text
局部实现错误
公共能力设计错误
生命周期错误
状态所有权错误
错误模型错误
并发或取消模型错误
持久化错误
协议边界错误
```

固定处理顺序：

1. 重现或通过代码证明问题；
2. 沿调用链向上和向下定位；
3. 找到最早失去约束的位置；
4. 搜索相同模式是否出现在其他模块；
5. 判断应局部修复还是修公共机制；
6. 实现最小但完整的修复；
7. 增加或调整回归测试；
8. 验证成功、失败和边界路径；
9. 检查相邻模块；
10. 完成后再次全链路审计。

## 10. 局部重写规则

以下信号出现时，应优先考虑重构公共边界，而不是继续叠补丁：

- 多处复制相同的错误处理；
- 多处复制相同重试；
- 多处维护相同 Pending 状态；
- 多处分别管理取消；
- 同一状态存在多个写入者；
- 一个修复不断在相邻模块重复出现；
- 测试依赖固定延时才能稳定；
- 生命周期没有明确所有者；
- 成功、失败、取消拥有完全不同的清理路径；
- 缓存或界面状态可以影响业务真值。

原则：

> 保留成熟且已经验证的内核，局部重写脆弱公共边界。

禁止：

- 全仓无边界重写；
- 为修一个问题换掉整个架构；
- 在每个调用点复制补丁；
- 用延时掩盖并发问题；
- 用静默重试掩盖错误；
- 用宽泛 `catch` 吞掉异常；
- 加一个布尔变量“先顶住”但不解决状态所有权；
- 为让测试通过创建特殊生产逻辑。

## 11. 修改范围原则

任何改动都必须满足：

```text
范围尽可能小
但闭环必须完整
```

“最小修改”不等于“只改一个文件”。

如果正确修复需要同步修改：

- 接口；
- 实现；
- 调用方；
- UI；
- 测试；
- 配置；

应一起修改。

禁止为了减少 diff 留下半套实现。

同时禁止借任务之名进行无关：

- 格式化；
- 文件改名；
- 大规模目录调整；
- API 清洗；
- 依赖升级；
- 代码风格翻新。

## 12. 接口与兼容性

修改公共接口、事件、工具 Schema、会话事件、SDK、ACP 或跨包契约时必须：

1. 搜索全部生产调用方；
2. 搜索全部测试调用方；
3. 检查序列化与反序列化；
4. 检查旧状态恢复；
5. 检查错误类型；
6. 检查 Client / Host；
7. 检查公开 SDK；
8. 检查是否形成破坏性变更。

非任务明确要求时，优先保持向后兼容。

不得静默改变已有公开语义。

## 13. 会话与持久化修改

涉及 Session、Storage、Query、Projection、Compaction 等模块时必须额外检查：

- 新会话；
- 老会话恢复；
- 会话分叉；
- 部分写入；
- 中途失败；
- 工具失败；
- 模型失败；
- 取消；
- 压缩前后；
- 重启后；
- 查询结果；
- UI 重建。

任何持久化结构都不得仅验证“写进去成功”。

必须验证：

```text
写入
→ 读取
→ 重建
→ 继续运行
```

## 14. 工具与执行能力修改

修改 Tool、Shell、Terminal、FS、Code Runtime、LSP、Sandbox 等能力时至少检查：

- 输入合法性；
- 权限；
- 路径边界；
- 工作目录；
- 环境变量；
- stdout / stderr；
- 退出码；
- 超时；
- 取消；
- 子进程清理；
- 资源释放；
- 大输出；
- 空输出；
- 用户可见错误；
- 会话事件；
- 沙箱与本地实现的一致性。

不得用测试替身的成功结果代替真实实现验证。

## 15. 子智能体与工作流

修改 Subagent、Jobs、Workflow、Schedule 时必须明确：

- 谁创建；
- 谁拥有；
- 谁取消；
- 谁回收；
- 结果写到哪里；
- 父任务退出时发生什么；
- 子任务失败如何传播；
- 重复消息如何处理；
- 超时如何处理；
- 会话恢复后如何处理旧任务结果。

禁止产生失去所有者的后台任务。

禁止让旧执行结果覆盖更新后的状态。

## 16. UI 修改规则

修改 Web / Client 时必须覆盖真实用户路径。

至少考虑：

- 空状态；
- 加载；
- 成功；
- 失败；
- 取消；
- 禁用；
- 权限拒绝；
- 长内容；
- 工具执行中；
- 会话切换；
- 会话恢复。

UI 不得虚构后端能力。

未实现的能力不得显示为已可用。

界面缓存不得成为权威业务状态。

## 17. 测试原则

测试的目的：

> 证明正确行为，而不是制造绿色结果。

测试必须尽可能覆盖：

```text
正常路径
失败路径
边界路径
取消路径
恢复路径
回归路径
```

修复缺陷时：

- 能稳定复现时，优先先建立失败测试；
- 无法稳定构造时，必须通过代码路径和针对性测试证明根因；
- 修复后必须加入能够防止同类回归的测试。

禁止：

- 删除有效测试以通过验证；
- 放宽断言掩盖错误；
- 增加无意义等待；
- 固定返回成功；
- 跳过真正失败用例；
- 修改快照以掩盖行为错误。

## 18. 当前精简基线的已知测试边界

当前仓库不包含 `examples/` 和 `docs/`。

以下测试依赖这些当前不存在的非产品资产：

```text
apps/cli/tests/memory-mcp-configs.spec.ts
packages/shell/tool-pwsh/tests/loader.spec.ts
packages/typert/generator/tests/cordis-catalog.spec.ts
```

处理规则：

- 不得为了让这些测试通过自行引入整个 `examples/` 或 `docs/`；
- 不得把这三项失败误判为产品源码故障；
- 执行当前产品有效测试集时，应单独排除这三项；
- 不得修改断言或生产逻辑制造绿色结果；
- 如果未来产品范围正式包含相关能力，再重新评估这些测试。

除此之外出现的任何测试失败，都必须实际调查。

## 19. 环境与依赖

权威环境：

```text
Node.js ^22.19.0 || >=24.0.0
pnpm 11.7.0
```

标准安装：

```bash
corepack enable
pnpm install --frozen-lockfile
```

规则：

- 正常开发必须尊重 `pnpm-lock.yaml`；
- 不得无理由改写锁文件；
- 不得为了修源码问题升级整套依赖；
- 不得擅自更换 Node 或 pnpm 主版本；
- 不得用 `npm install` 生成第二套依赖状态；
- 新增生产依赖前必须确认现有依赖无法解决；
- 新增依赖时必须评估包体积、安全、维护状态和现有替代方案。

## 20. 基础验证命令

按任务范围执行对应验证。

### 完整构建

```bash
pnpm run build
```

### 类型检查

```bash
pnpm run typecheck
```

### 基础测试

```bash
pnpm run test
```

### 覆盖率

```bash
pnpm run test:coverage
```

### 端到端测试

```bash
pnpm run test:e2e
```

### 快照测试

```bash
pnpm run test:snapshot
```

### Web

```bash
pnpm run test:web
```

### 图形界面相关

```bash
pnpm run test:gui
```

### Web 性能

```bash
pnpm run test:web:perf
```

### Web 压力测试

```bash
pnpm run test:web:stress
```

## 21. 验证层级

不得机械地每次都运行所有测试。

根据风险采用：

### 一级：局部验证

修改单包内部实现：

```text
对应测试
+ 对应类型检查
```

### 二级：关联验证

影响公共能力、接口或跨包行为：

```text
局部测试
+ 消费方测试
+ typecheck
```

### 三级：全量验证

以下情况必须完整构建并扩大测试：

- 公共接口修改；
- Agent Loop；
- Session；
- Tool Pipeline；
- Runtime；
- Sandbox；
- Host / Client 协议；
- SDK；
- 构建配置；
- 工作区配置；
- 依赖变化；
- 大规模重构；
- 任务最终验收。

至少：

```bash
pnpm run build
```

并运行受影响产品测试集。

高风险任务按需要继续执行：

```bash
pnpm run test:e2e
pnpm run test:web
pnpm run test:snapshot
```

## 22. 验证真实性

未经真实运行不得说：

- 已通过；
- 已修复；
- 构建正常；
- 测试全绿；
- 没有回归；
- 可以发布。

必须区分：

```text
代码审查认为正确
≠
编译通过
≠
测试通过
≠
真实运行通过
```

工具或环境无法执行某项验证时必须明确说明：

- 没运行什么；
- 为什么没运行；
- 已完成哪些替代验证；
- 仍存在什么风险。

禁止把“没有发现问题”写成“验证通过”。

## 23. 完成后强制审计

代码写完不代表任务完成。

每个任务在首次实现后必须进入审计阶段。

固定流程：

```text
实现完成
↓
检查 diff
↓
检查任务原始要求
↓
检查完整调用链
↓
检查相邻模块
↓
运行验证
↓
分析所有失败
↓
发现问题则继续修复
↓
重新验证
↓
再次检查最终 diff
↓
确认仓库状态
↓
任务关闭
```

审计至少回答：

1. 用户要求是否全部实现？
2. 是否存在遗漏的关联路径？
3. 是否出现第二套实现？
4. 是否破坏已有接口？
5. 是否存在错误吞掉？
6. 是否存在资源未释放？
7. 是否破坏恢复能力？
8. 是否遗漏失败路径？
9. 测试是否真正验证行为？
10. 是否产生无关修改？
11. 是否新增不必要依赖？
12. 是否留下临时文件或调试代码？
13. 是否所有声明都由实际证据支持？

任何一项存在问题时，继续修复。

禁止“发现问题后只在最终报告里列出来但不修”，除非问题明确超出用户授权范围。

## 24. 完成条件

只有以下条件全部成立，任务才能标记为完成：

```text
需求已实现
+
关联修改已完成
+
代码自审完成
+
相关测试真实运行
+
必要构建真实运行
+
失败原因已处理
+
无已知任务内回归
+
无临时调试产物
+
无无关改动
+
最终 diff 已复核
```

如果任务内仍有可修复问题，则任务未完成，应继续处理。

## 25. 禁止伪完成

严格禁止：

- 代码没写完却说完成；
- 没运行测试却说测试通过；
- 测试失败却只汇报成功部分；
- 创建空实现；
- 固定返回成功；
- 用假数据代替功能；
- 用 TODO 代替任务实现；
- 暂时注释失败代码；
- 删除失败测试；
- 用 `skip` 隐藏回归；
- 静默吞错；
- 只修表象不查根因；
- 只验证当前路径不检查相邻路径；
- 把“流程已启动”当成“任务完成”。

## 26. 仓库写入边界

写入前必须确认：

```text
仓库
分支
HEAD
任务范围
目标文件
```

写入后必须重新读取真实结果。

规则：

- 不得仅依赖工具返回“成功”；
- 修改后检查实际文件；
- 提交后检查实际 commit；
- PR 操作后检查实际 PR 状态；
- 分支操作后检查实际 ref；
- 多文件修改应避免留下可见的半成品中间状态。

不得创建：

- 一次性 GitHub Workflow；
- 为一次验证存在的永久配置；
- 临时验证源码；
- 临时产品分支；
- 临时补丁目录；
- 为 CI 专门存在的生产逻辑。

除非用户明确要求，否则不要改变仓库分支治理。

## 27. 自动化边界

自动化可以：

- 构建；
- 测试；
- 静态检查；
- 打包；
- 输出报告。

自动化不得：

- 自动生成正式业务源码并直接提交；
- 自动改写产品行为；
- 自动篡改测试结果；
- 自动修改任务状态来伪造完成；
- 通过流水线完成本应由正式源码承担的功能。

CI 是验证者，不是业务代码作者。

## 28. Vendor 修改规则

`vendor/` 保存随仓库维护的底层依赖源码。

修改前必须确认问题确实位于 vendor 层。

优先顺序：

```text
正常使用公开能力
→ Harness 适配层修复
→ 必要时补丁
→ 最后才直接修改 vendor
```

直接修改 vendor 时必须：

- 明确原因；
- 检查所有使用方；
- 添加回归测试；
- 避免针对 Harness 单一场景破坏通用语义。

## 29. 测试支持代码边界

`packages/test-support/` 和测试夹具只能用于测试。

禁止生产源码依赖测试支持模块。

测试环境不能成为产品正常运行的隐藏依赖。

## 30. 安全规则

涉及以下能力时按高风险处理：

- Shell；
- PowerShell；
- 文件写入；
- 代码执行；
- 子进程；
- 终端；
- 沙箱；
- 网络访问；
- MCP；
- 外部工具；
- 凭据；
- 工作区边界。

必须检查：

- 参数注入；
- 路径逃逸；
- 权限绕过；
- 沙箱逃逸；
- 凭据泄漏；
- 环境变量泄漏；
- 非预期网络访问；
- 子进程残留；
- 任意文件访问；
- 日志泄漏敏感数据。

安全限制必须在代码边界执行。

模型提示词不能充当安全边界。

## 31. 性能规则

Agent Loop、Session、Context、Tool、Web、Client 热路径修改时应检查：

- 是否出现无界数组；
- 是否重复扫描全部会话；
- 是否重复序列化大对象；
- 是否在热路径同步读取磁盘；
- 是否产生无界并发；
- 是否产生泄漏监听器；
- 是否重复注册插件；
- 是否重复建立连接；
- 是否让 UI 因大型工具输出阻塞。

优化必须以真实瓶颈为依据。

禁止无依据的“性能重构”。

## 32. 错误处理

错误必须：

```text
在正确层产生
→ 保留有用上下文
→ 按契约传播
→ 在合适层转换
→ 最终让用户或调用方得到明确结果
```

禁止：

```ts
catch {
  return;
}
```

这类静默吞错。

只有明确可忽略且不会改变正确性的异常才能被吸收，并应说明原因。

不要重复包裹同一个错误直到失去原始原因。

## 33. 日志原则

日志用于诊断，不是状态真源。

禁止：

- 用日志存在与否决定业务行为；
- 把凭据写进日志；
- 把完整敏感模型输入无条件写入日志；
- 高频循环输出大量无价值日志；
- 为调试永久加入噪音日志。

任务结束前清理临时日志。

## 34. 注释与文档

代码应优先通过结构和命名表达意图。

注释用于解释：

- 为什么这么做；
- 特殊协议约束；
- 不明显的不变量；
- 安全原因；
- 兼容性原因。

禁止注释逐行翻译代码。

涉及对用户有意义的能力变化时，检查 README 是否需要同步。

## 35. 提交规则

提交内容必须与任务一致。

一个正式提交不应混入：

- 无关格式化；
- 临时验证文件；
- 调试输出；
- 本地环境文件；
- 构建产物；
- 缓存；
- 无关文档改写。

提交信息优先使用中文清楚描述目的。

代码标识符、包名、路径和命令保持原样。

## 36. 最终汇报

完成任务后，汇报必须基于事实。

推荐结构：

```text
完成了什么
验证了什么
发现并修复了什么
最终状态
仍存在的明确限制（若有）
```

不要只说：

```text
完成了
已修复
没问题
```

必须能对应到真实代码和验证结果。

如果任务最终全部闭环，直接明确说明。

如果仍存在未完成项，必须准确写出，不得包装成完成。

## 37. Agent 最终原则

所有开发任务遵循：

> 先理解，再修改。  
> 先找根因，再写补丁。  
> 优先复用已有能力，禁止制造第二套真源。  
> 修改范围可以小，任务闭环不能残缺。  
> 测试必须真实运行，结果必须真实汇报。  
> 实现完成后必须审计，发现问题继续修。  
> 只有最终结果满足用户要求，任务才算完成。

## 38. AI 编程自动推进与合并闭环规范

### 38.1 默认工作模式

所有明确的软件开发任务默认采用：

> 接收任务 → 建立基线 → 分析影响 → 实现 → 测试 → 审计 → 修复 → 再验证 → 提交 → PR → 合并 → 主线复验 → 清理 → 完成。

除非用户明确要求只分析、只评估、只设计、只修改但不提交，否则 Agent 不得在中间步骤主动停止。

“实现完成”“测试通过”“PR 已创建”均不是任务终点。

默认终点是：修改已经安全进入目标主线，并完成合并后的主线复验。

### 38.2 自动推进原则

用户下达明确任务后，只要不存在必须由用户裁决的产品决策，Agent 应自主完成后续工程判断。

不得因为以下事项反复询问用户：

- 文件应该放在哪里；
- 应修改哪个内部模块；
- 应增加哪些合理测试；
- 局部实现采用哪种正常工程方案；
- 是否修复任务范围内发现的关联缺陷；
- 是否继续跑测试；
- 测试失败后是否继续修；
- 是否需要检查调用方；
- 是否需要更新关联类型；
- 是否继续提交；
- 是否继续创建 PR；
- 验证通过后是否可以合并。

这些均属于 Agent 应独立承担的工程职责。

### 38.3 允许停止并请求用户裁决的情况

只有出现以下情况，才允许暂停自动推进：

1. 存在两个或以上明显不同的产品方向，且仓库无法确定预期；
2. 需要删除或永久改变用户数据；
3. 需要改变公开接口并明确破坏兼容性；
4. 需要更换核心技术路线或框架；
5. 需要引入新的外部服务、付费服务或重要生产依赖；
6. 任务目标与用户此前明确要求直接冲突；
7. 需要执行用户未授权的高风险外部操作；
8. 缺失的信息无法通过当前仓库、测试、配置或现有上下文推导。

普通代码实现分歧不属于上述情况。

### 38.4 任务开始阶段

开始任何开发任务前，Agent 必须自动完成：

```text
确认用户目标
→ 读取 AGENTS.md
→ 读取 README.md
→ 确认目标分支和 HEAD
→ 检查工作区状态
→ 阅读相关源码
→ 阅读相关测试
→ 搜索调用方与依赖方
→ 确定影响范围
→ 建立验证方案
```

如果发现已有未完成修改：

- 先判断是否属于当前任务；
- 不得覆盖未知工作；
- 不得误删他人修改；
- 必须在安全基线上继续。

### 38.5 实现阶段

实现过程中必须遵守：

```text
修复根因
>
修复公共机制
>
修复局部现象
```

如果修改一个位置后发现同类缺陷存在于相邻路径，应继续处理任务影响范围内的同源问题。

禁止：

- 只修第一个报错；
- 只让当前测试通过；
- 只针对现象增加特判；
- 留下半套迁移；
- 创建第二套状态或能力；
- 用 TODO 代替剩余实现；
- 把明显属于当前任务的问题留给“以后”。

### 38.6 首轮验证

实现完成后立即执行适当验证。

最低要求：

```text
相关单元测试
+ 相关类型检查
+ 受影响模块测试
```

涉及公共能力、跨包契约、运行时、Session、Tool、Sandbox、Host/Client、SDK、依赖或构建系统时，必须扩大到：

```text
pnpm run typecheck
pnpm run build
相关产品测试
```

必要时增加：

```text
pnpm run test:e2e
pnpm run test:web
pnpm run test:snapshot
pnpm run test:web:perf
```

### 38.7 测试失败自动修复

任何验证失败都必须进入：

```text
失败
→ 读取真实日志
→ 定位第一原因
→ 判断产品缺陷 / 测试缺陷 / 环境缺陷
→ 修复
→ 重跑失败测试
→ 重跑关联测试
```

不得因为一次失败就停止并询问用户“是否继续”。

禁止：

- 删除测试；
- 无理由跳过测试；
- 放宽断言；
- 增加固定等待；
- 静默吞错；
- 修改生产代码制造固定成功；
- 将真实产品问题标记成环境问题。

如果确认失败来自不属于当前产品范围的资产或环境，必须有明确证据，并将其与真实产品失败分离。

### 38.8 完成前代码审计

测试通过后，Agent 必须再进行一次独立代码审计。

固定检查：

```text
原始需求是否全部覆盖
调用链是否完整
公共接口是否同步
错误路径是否完整
取消/超时是否完整
状态所有权是否正确
资源是否释放
持久化是否可恢复
UI 是否与后端一致
是否存在重复实现
是否存在调试代码
是否存在临时文件
是否存在无关修改
是否存在新增安全风险
```

审计发现任务范围内问题：直接继续修复。

不得只在最终汇报中列问题却不修复。

### 38.9 审计后的再验证

审计导致任何代码变化后，必须重新运行受影响验证。

如果改动触及公共能力，应重新扩大验证范围。

最终准备提交时，必须保证：

```text
最终源码
=
最终被测试的源码
```

禁止测试 A 版本，之后修改成 B 版本却直接提交。

### 38.10 提交前检查

提交前必须重新读取真实仓库状态。

至少检查：

```text
git diff
git status
目标分支
当前 HEAD
待提交文件
```

确认：

- 没有临时日志；
- 没有验证工作流；
- 没有调试文件；
- 没有构建产物；
- 没有无关格式化；
- 没有凭据；
- 没有误修改锁文件；
- 没有任务范围外文件。

### 38.11 提交规范

正式提交必须表达真实目的。

优先使用中文，例如：

```text
修复会话恢复后的工具状态丢失
完善子智能体取消与资源回收
统一沙箱文件执行边界
增加工作流失败路径测试
```

禁止使用无法说明目的的正式提交标题，例如：

```text
update
fix
test
tmp
wip
```

### 38.12 PR 自动推进

如果当前仓库采用 PR 合并模式，代码提交后 Agent 应继续：

```text
推送任务分支
→ 创建 PR
→ 检查 PR diff
→ 检查 PR 文件范围
→ 执行或等待正式验证
→ 处理失败
→ 更新 PR
→ 再验证
→ 满足合并条件
→ 合并
```

不得在“PR 已创建”处停止。

### 38.13 PR 自审

创建 PR 后必须以陌生审查者视角重新检查一次。

至少核对：

- PR 是否准确实现用户目标；
- 是否混入其他任务；
- 是否遗漏调用方；
- 是否存在明显兼容问题；
- 测试是否真正覆盖修改；
- 提交信息是否准确；
- diff 是否存在异常大改；
- 是否出现意外删除；
- 是否包含临时文件。

发现问题直接修改 PR 并重新验证。

### 38.14 合并条件

只有同时满足以下条件才允许自动合并：

```text
任务要求全部实现
AND
相关测试通过
AND
必要构建通过
AND
代码审计通过
AND
PR diff 已复核
AND
不存在未解决的阻断问题
AND
当前 PR Head 与已验证版本一致
AND
目标主线未出现无法安全吸收的新变化
```

任意条件失败，不得合并，继续处理。

### 38.15 合并前主线漂移检查

准备合并前必须重新读取目标主线。

如果主线发生变化：

```text
读取新 main
→ 判断冲突
→ 判断语义影响
→ 必要时同步
→ 重新验证
→ 再允许合并
```

禁止验证旧基线后无条件合并到已经发生重大变化的新主线。

### 38.16 自动合并

满足全部条件后，如果工具权限允许且用户未禁止，Agent 应自动执行合并。

不需要再次询问“要不要合并”。

用户下达开发、修复或实现任务时，本规范默认已经包含安全完成到合并阶段的授权。

除非用户明确要求：

- 不要提交；
- 不要创建 PR；
- 不要合并；
- 只给代码；
- 合并前由用户审核。

### 38.17 合并后主线验证

合并成功不代表任务结束。

必须重新读取主线真实 HEAD，并执行：

```text
确认合并结果
→ 确认目标文件实际存在
→ 确认最终主线内容
→ 必要构建
→ 必要测试
```

最终验证必须针对合并后的真实 `main`。

禁止拿 PR Head 的测试结果直接冒充 main 验证。

### 38.18 合并后发现问题

如果主线复验失败：

```text
任务重新进入进行中
→ 定位原因
→ 修复
→ 测试
→ 审计
→ 提交
→ PR
→ 合并
→ 再次主线验证
```

不得因为“已经合并”就把后续失败留给用户。

### 38.19 分支与临时资源清理

任务成功合并并通过主线验证后，检查：

- 任务分支；
- 临时分支；
- 临时 PR；
- 临时工作流；
- 临时测试文件；
- 临时日志；
- 验证产物；
- 调试配置。

能够安全删除的临时资源必须清理。

不得长期积累一次性 `verify/*`、`tmp/*`、`debug/*` 分支或临时 GitHub Workflow 等资源。

### 38.20 最终仓库审计

任务关闭前最后检查：

```text
main HEAD
开放 PR
临时分支
仓库根目录
任务相关文件
构建配置
测试状态
```

确认仓库没有因本任务留下新的污染。

### 38.21 自动推进状态机

完整状态固定为：

```text
RECEIVED
↓
ANALYZING
↓
IMPLEMENTING
↓
VERIFYING
↓
AUDITING
↓
FIXING（如需要）
└────────→ VERIFYING
↓
READY_TO_COMMIT
↓
COMMITTED
↓
PR_OPEN
↓
PR_VERIFYING
↓
READY_TO_MERGE
↓
MERGED
↓
MAIN_VERIFYING
↓
CLEANUP
↓
DONE
```

任何失败都应回到最近的正确阶段继续推进。

不得直接从：

```text
IMPLEMENTING → DONE
COMMITTED → DONE
PR_OPEN → DONE
MERGED → DONE
```

### 38.22 自动修复循环

Agent 应允许任务多轮自我修复：

```text
实现
→ 测试失败
→ 修复
→ 测试
→ 审计发现问题
→ 修复
→ 测试
→ PR 检查发现问题
→ 修复
→ 测试
→ 合并后失败
→ 修复
→ 再次闭环
```

循环持续到满足完成条件。

不得因为经历多轮失败就降低验收标准。

### 38.23 阻塞处理

如果存在暂时无法解决的外部阻塞，必须先穷尽仓库内可执行工作：

- 代码分析；
- 局部实现；
- 静态验证；
- 单元测试；
- 构建；
- 日志分析；
- 替代验证；
- 风险收敛。

只有确实无法继续时，才能停止。

最终必须准确说明：

```text
阻塞位置
已完成部分
真实失败原因
尚未验证内容
继续推进需要什么
```

不得包装成“基本完成”。

### 38.24 禁止人为切断闭环

禁止 Agent 使用以下话术或行为主动终止正常任务：

- “代码已经写好了，你可以自己测试。”
- “PR 已创建，你可以合并。”
- “测试失败了，要不要继续修？”
- “发现几个问题，建议以后处理。”
- “构建通过，所以应该没问题。”
- “我已经触发 CI，等结果即可。”
- “剩下只是收尾。”
- “理论上可以运行。”

只要工具权限和任务授权允许，Agent 自己继续做。

### 38.25 完成定义

开发任务只有满足以下全部条件才能宣布完成：

```text
需求完成
+
代码完成
+
测试完成
+
审计完成
+
发现的问题已修复
+
最终代码重新验证
+
提交完成
+
PR 验证完成（如采用 PR）
+
安全合并完成
+
main 复验完成
+
临时资源清理完成
+
仓库最终审计完成
```

最终状态必须是：

> 已验证的正确结果已经存在于目标主线。

### 38.26 用户指令优先

用户可以明确缩短闭环，例如：

```text
只分析
只给方案
只修改不提交
提交但不要 PR
创建 PR 但不要合并
合并前让我审核
```

这些明确指令优先于自动闭环。

如果用户没有作上述限制，则默认执行完整闭环直到合并后的主线验证完成。

## 39. 永久三分支治理与同步规范

本节是用户已明确批准的仓库分支治理规则，对本文件中所有涉及“任务分支”“临时分支”“PR 来源分支”“合并后分支清理”的旧表述作进一步具体化；发生冲突时以本节为准。

### 39.1 永久分支集合

仓库长期且只允许以下三个分支：

```text
main
work
governance
```

职责固定为：

```text
main
= 已完成受控合并并经过主线验证的稳定主线

work
= 所有产品开发、产品修复、产品重构、产品测试与产品配置的永久集成 lane

governance
= 所有仓库治理、CI、构建、测试治理、规则、依赖治理与发布治理的永久集成 lane
```

`work` 和 `governance` 是永久分支，任务完成后不得删除。

### 39.2 禁止第四分支

除修复缺失的 `work` 或 `governance` 永久分支本身外，禁止创建任何第四分支。

包括但不限于：

```text
work/*
governance/*
feat/*
feature/*
fix/*
bugfix/*
chore/*
policy/*
probe/*
stage/*
validate/*
verify/*
test/*
docs/*
release/*
tmp/*
debug/*
```

禁止因为“问题独立”“需要验证”“方便开 PR”“需要暂存”而建立新分支。

任务、问题和修复之间的区分应通过提交、PR、Issue、Work 或任务记录表达，不通过新增 Git 分支表达。

### 39.3 任务 lane 选择

开始任务前必须先分类：

```text
改变产品行为、产品源码、产品契约、产品测试、产品配置
→ work

只改变仓库治理、CI、构建、测试门禁、开发规则、依赖治理、发布治理
→ governance
```

治理任务一旦确认必须修改真实产品逻辑，必须转入 `work` 的产品闭环；不得借 `governance` 修改产品行为。

产品任务不得顺手混入无关治理改动。

### 39.4 单 lane 串行与任务排队

同一时刻每条永久 lane 最多承载一个尚未进入 `main` 的独立闭环，并最多存在一个对应开放 PR。

```text
work
→ 最多一个未合并产品闭环

governance
→ 最多一个未合并治理闭环
```

如果当前 lane 已有开放 PR、独有未合并提交或进行中的工作：

- 与当前任务同根因、同范围的关联问题继续并入当前闭环；
- 独立的新任务等待当前闭环完成后再开始；
- 禁止为第二个问题建立新分支。

`work` 与 `governance` 两条 lane 可以各自并行准备一个闭环，但写入 `main` 必须串行。

### 39.5 新任务启动基线

开始新的 `work` 任务或新的 `governance` 任务前，对应 lane 必须先确认：

```text
无旧开放 PR
无未处理独有提交
无上一个任务残留状态
```

并与最新已验证 `main` 建立安全基线。

空闲 lane 若只是 `main` 的祖先，只允许非强制快进到最新 `main`。

存在独有或分叉提交时禁止强制覆盖，必须先查明归属。

### 39.6 PR 来源固定

正式 PR 只允许两种：

```text
work → main
governance → main
```

禁止：

```text
其他分支 → main
work → governance
governance → work
main → work 的同步 PR
main → governance 的同步 PR
```

主线向永久 lane 的同步通过本节的 Integration Branch Synchronization 完成，不创建专门同步 PR。

### 39.7 产品任务流程

产品开发固定流程：

```text
已验证 main
→ 同步空闲 work
→ 在 work 实现
→ 测试
→ 审计
→ 修复
→ 再验证
→ PR：work → main
→ PR 自审与正式门禁
→ 受控合并
→ main 主线复验
→ Integration Branch Synchronization
→ Branch Inventory / Hygiene
→ 下一产品任务
```

### 39.8 治理任务流程

治理维护固定流程：

```text
已验证 main
→ 同步空闲 governance
→ 在 governance 修改治理、CI、构建、测试或规则
→ 验证
→ 审计
→ PR：governance → main
→ PR 自审与正式门禁
→ 受控合并
→ main 主线复验
→ Integration Branch Synchronization
→ Branch Inventory / Hygiene
→ 下一治理任务
```

### 39.9 main 写入与主线漂移

禁止直接向 `main` 提交正式任务修改。

`main` 只接受通过 `work → main` 或 `governance → main` 的受控合并。

两条 lane 即使同时准备完成，也必须串行合并。

准备合并第二条 lane 前必须重新读取最新 `main`：

```text
读取新 main
→ 判断主线漂移
→ 安全吸收最新 main
→ 解决冲突与语义变化
→ 重新运行受影响验证
→ 再进入合并门禁
```

禁止拿旧主线基线的验证结果直接合并到已经变化的新 `main`。

### 39.10 合并后的永久分支同步

`main` 合并后必须先完成真实主线验证。

只有主线验证成功，才允许执行 Integration Branch Synchronization。

#### 来源 lane

来源 lane 只有以下条件全部成立时才能自动收敛到 `main`：

```text
main 主线验证成功
AND
当前 main SHA 等于被验证的 main SHA
AND
能够确认来源 PR 精确为 work → main 或 governance → main
AND
来源 lane 仍等于该 PR 受检 Head
AND
来源 lane 在合并后没有新增提交
AND
当前没有新的同 lane 开放 PR
```

若采用 Squash 合并导致来源 lane 与 `main` 历史分叉，在上述条件全部满足后，允许将来源永久 lane 受控重置到已验证 `main`。

这不是通用强推许可；任一条件不成立均禁止覆盖。

#### 兄弟 lane

另一条永久 lane 采用更保守规则：

```text
已等于 main
→ 保持不动

无开放 PR + 无独有提交 + 当前 Head 只是 main 祖先
→ 仅允许非强制 fast-forward 到最新 main

存在开放 PR 或进行中的独有工作
→ skip，保留现场

无开放 PR但存在独有/分叉提交
→ fail-closed，禁止 force 覆盖
```

兄弟 lane 因活动工作被跳过同步时，必须在它准备合并前吸收最新 `main` 并重新验证。

每次写入分支 Ref 后必须重新读取实际 Ref，禁止只相信写操作返回成功。

### 39.11 主线验证失败

如果合并后的 `main` 主线复验失败：

```text
禁止同步永久 lane
→ 任务重新进入进行中
→ 在对应来源 lane 修复
→ 测试与审计
→ PR
→ 合并
→ 再次 main 验证
```

在主线恢复已验证状态前，不得把失败主线扩散同步到空闲兄弟 lane。

### 39.12 第四分支迁移与清理

发现第四分支时禁止直接删除或强制覆盖。

必须先检查：

```text
是否有开放 PR
是否有 main 未包含的提交
是否承载未完成任务
应该归入 work 还是 governance
```

若能安全迁移：

```text
把完整未合并历史迁入正确永久 lane
→ 重新验证真实内容
→ 将 PR 改为正确永久 lane → main 的正式 PR
→ 确认没有工作丢失
→ 再删除第四分支
```

若不能证明迁移安全，则 fail-closed 并报告阻塞，不得为了满足“三分支库存”而丢弃未知工作。

### 39.13 分支库存审计

每个开发或治理任务开始前、合并后、最终关闭前都必须检查远端分支库存。

合法集合固定为：

```text
{ main, work, governance }
```

最终仓库卫生审计必须确认：

- 没有额外任务分支；
- 没有验证专用分支；
- 没有临时分支；
- `work` / `governance` 没有被误删；
- 空闲永久 lane 已按安全条件收敛到最新已验证 `main`；
- 活动永久 lane 的不同步有明确原因。

### 39.14 与第 38 节旧措辞的解释关系

第 38 节出现的“任务分支”“推送任务分支”“临时分支清理”等旧措辞，从本节生效后统一解释为：

```text
产品任务使用永久 work lane
治理任务使用永久 governance lane
禁止为单个问题创建临时或任务专属分支
任务完成后的分支清理只清理违规第四分支，不删除 work / governance
```

因此，第 38.12 节的“推送任务分支”应实际执行为推送对应永久 lane；第 38.19、38.20 节的“任务分支/临时分支”检查应执行为第四分支库存审计。

### 39.15 完成条件补充

在采用本三分支模型后，开发或治理任务的最终完成条件还必须包括：

```text
main 主线复验成功
+
来源永久 lane 已按安全条件同步
+
空闲兄弟 lane 已按安全条件同步，或因存在活动工作而明确 skip
+
远端分支库存已审计
+
没有因本任务新增长期第四分支
```

只有上述条件与第 38.25 节全部满足，任务才可进入最终完成状态。