# AGENTS.md

## 1. 权威、目标与适用范围

本仓库是 DeepSeek Harness 的精简二次开发基线。本文件是仓库级开发代理的最高长期规则；`agent.md` 只提供高频执行入口，不得与本文件冲突。

动态事实不写死在长篇文字里，分别由以下机器文件负责：

```text
.github/governance/repository-policy.json
= 分支、PR、main 写入、合并与主线闭环规则

.github/task-control/policy.json
= 任务状态模型与最终交付判定规则

.github/task-control/work.json
= 当前 product/work 永久集成分支的任务状态

.github/task-control/governance.json
= 当前 governance 永久集成分支的任务状态
```

发生冲突时固定按以下顺序处理：

```text
用户当前最新明确指令
>
用户已经明确批准的任务范围、设计与验收要求
>
本文件中的仓库级不变量和授权边界
>
repository-policy.json 与 task-control 机器状态
>
README.md、package.json、pnpm-workspace.yaml、tsconfig 与测试/构建配置
>
当前功能包契约、测试与现有源码
>
历史实现习惯
```

机器文件负责回答“当前真实状态是什么”；本文件负责回答“允许怎么推进、什么时候才算完成”。禁止用历史代码或旧习惯覆盖用户明确需求。

## 2. 明确开发任务的默认授权

用户下达明确的软件开发、修复、重构、测试、治理或实现任务后，默认授权代理在本仓库内完成正常工程闭环，不需要在以下步骤重复请求许可：

- 读取仓库、分支、提交、PR、检查结果和日志；
- 在正确的永久集成分支修改任务范围内文件；
- 增加或调整必要测试；
- 修复任务范围内发现的同根因缺陷；
- 运行、重跑和扩大必要验证；
- 提交并推送当前永久集成分支；
- 创建或更新 `work → main` / `governance → main` PR；
- 检查 PR 差异和正式门禁；
- 门禁失败后定位根因、修复并重新验证；
- 满足全部条件后执行受控合并；
- 合并后验证真实 `main`；
- 按机器策略同步 `work` / `governance`；
- 执行分支库存与卫生审计；
- 在任务真正交付前持续推进。

上述授权明确消除旧规则中“不得自行创建未经要求的 PR”与“必须自动推进到合并”的二义性：**明确开发任务默认包含使用既有三分支治理完成提交、PR、验证、合并和主线复验的授权。**

但以下事项仍必须由用户明确裁决：

1. 改变产品定位或核心技术路线；
2. 删除或永久改变用户数据；
3. 明确破坏公开接口或兼容性；
4. 引入新的外部服务、付费服务或重要生产依赖；
5. 大规模替换框架或成熟持久化内核；
6. 暴露、传输或重置凭据与敏感信息；
7. 强制覆盖无法证明归属的远端提交或分支历史；
8. 任务目标与用户此前明确要求直接冲突且仓库无法消解；
9. 缺失信息无法通过仓库、测试、配置、PR 或现有上下文推导。

创建或修改永久 GitHub Workflow 本身属于仓库治理变更，只在任务确实需要治理调整时进行；**使用已经批准存在的 Workflow 完成当前任务不需要再次授权。**

## 3. 强制启动顺序

开始任何编码、修复、重构、测试、治理或发布工作前，依次读取并确认：

```text
1. AGENTS.md
2. agent.md
3. .github/governance/repository-policy.json
4. .github/task-control/policy.json
5. 当前执行分支对应的 .github/task-control/<lane>.json
6. README.md
7. 根 package.json、pnpm-workspace.yaml、tsconfig 与相关配置
8. 当前 main / work / governance 真实 Ref
9. 当前开放 PR、对应 Head、正式检查与 main 状态
10. 任务相关源码、测试、调用方、消费者和最近变更
```

开始修改前必须回答：

```text
已有能力是否已经存在？
是否存在可复用的公共机制？
如果新增，会不会形成第二套真源？
当前永久集成分支是否已经承载未完成闭环？
```

如果当前分支已经存在开放 PR、独有提交，或任务状态为 `IN_PROGRESS` / `IMPLEMENTED` 但仓库还未达到 `DELIVERED`，必须先恢复并完成现有闭环；禁止把独立新任务叠进同一永久分支。

## 4. 轻量任务状态真源

任务状态只使用：

```text
.github/task-control/policy.json
.github/task-control/work.json
.github/task-control/governance.json
```

禁止再建立第二套 `ACTIVE_TASK`、临时任务目录、聊天内隐式状态或仅靠日志判断任务完成度。

每条永久集成分支只有一个当前状态文件。静态状态固定为：

```text
IDLE
→ IN_PROGRESS
→ IMPLEMENTED
```

`phase` 用于记录当前工作阶段，允许值由 `.github/task-control/policy.json` 定义，例如：

```text
ANALYZING
IMPLEMENTING
VERIFYING
AUDITING
FIXING
IMPLEMENTED
```

规则：

- 新任务的第一批持久化修改必须把对应状态文件切到 `IN_PROGRESS`，记录 `taskId`、目标和验证计划；
- 任务处理中可在自然提交点更新 `phase`，不要求为了聊天进度制造空提交；
- 只有实现、相关验证、代码审计与再验证均完成后，才能把静态状态改为 `IMPLEMENTED`；
- `IMPLEMENTED` 只表示候选分支已经达到可进入 PR/合并门禁的状态，**绝不等于最终交付**；
- `IMPLEMENTED` 后如果源码、测试、配置或治理逻辑再次变化，必须重新确认验证仍对应最终 Head；必要时回到 `IN_PROGRESS`；
- PR Policy 必须使用可信 `main` 上的任务控制代码检查候选分支状态，候选 PR 不能自行决定自己的合并资格；
- 不得自动改写状态来制造完成，也不得用状态文件替代真实代码和测试证据。

## 5. 最终完成只有一个机器判定

仓库有效状态由真实 GitHub 状态计算，不由代理自行解释。固定状态为：

```text
BRANCH_HYGIENE_BLOCKED
PR_ACTIVE
MAIN_VERIFYING
SYNC_PENDING
DELIVERY_VERIFYING
DELIVERED
```

只有 `DELIVERED` 允许向用户声明“任务完成”“已经闭环”或同义最终结论。

最终交付前优先执行：

```bash
node .github/governance/repository-state.mjs assert-delivered
```

如果当前执行环境无法直接运行该命令，也必须通过真实 GitHub 数据逐项得到完全等价的结果；不得用推测替代。

`DELIVERED` 必须同时证明：

```text
没有开放的永久集成分支 PR
+
当前 main 的 main-verification = success
+
当前 main 的 delivery-ready = success
+
work / governance 已按安全规则收敛或处于机器允许的最终状态
+
没有非法第四分支
```

其中 `delivery-ready` 只能在：

```text
真实 main 验证成功
→ 永久集成分支同步成功
→ 最终 Branch Hygiene 成功
```

之后发布。

过程中的 `IMPLEMENTED`、测试通过、提交完成、PR 已创建、PR 可合并、Controlled Merge 已执行、`main-verification` 已成功，全部都不是最终完成信号。

代理可以在长任务中报告真实进展，但若状态不是 `DELIVERED`，必须明确它是进展而非交付，且应继续执行当前仍可完成的工作。

## 6. 永久三分支治理

仓库长期且只允许：

```text
main
work
governance
```

职责：

```text
main
= 已通过受控合并、主线验证和最终交付闭环的稳定主线

work
= 产品源码、产品修复、产品重构、产品测试和产品配置的永久集成分支

governance
= AGENTS、GitHub Workflow、构建、测试治理、规则、依赖治理和发布治理的永久集成分支
```

固定规则：

- 禁止创建第四分支，包括 `feat/*`、`fix/*`、`work/*`、`governance/*`、`verify/*`、`tmp/*` 等；
- 产品 PR 只能是 `work → main`；治理 PR 只能是 `governance → main`；
- 同一时刻每条永久集成分支最多一个未合并闭环和一个开放 PR；
- `work` 与 `governance` 可各自准备一个闭环，但 `main` 写入必须串行；
- 禁止直接向 `main` 提交正式任务修改；
- 新任务开始前，对应永久集成分支必须没有旧开放 PR、未知独有提交或未关闭状态；
- 空闲分支只是 `main` 祖先时，只允许安全快进；
- 存在独有或分叉历史时，禁止无证据强制覆盖；
- Squash 合并后的来源分支只有在来源 PR、受检 Head、当前 Ref、主线验证均能精确绑定时，才允许受控重置到已验证 `main`；
- 兄弟分支有活动 PR 时保留现场；无活动 PR 且只有落后历史时安全快进；存在未知独有历史时关闭式阻断。

产品任务：

```text
最新已验证 main
→ work
→ IN_PROGRESS
→ 实现 / 测试 / 审计 / 修复 / 再验证
→ IMPLEMENTED
→ work → main PR
→ PR Policy + Repository Gates
→ Controlled Merge
→ Main Verification
→ Integration Branch Synchronization
→ Branch Hygiene
→ delivery-ready
→ DELIVERED
```

治理任务同理，只把 `work` 换成 `governance`。

## 7. 自动推进与失败恢复

明确开发任务的默认执行链：

```text
接收任务
→ 建立真实基线
→ 记录 IN_PROGRESS
→ 分析完整影响链
→ 实现
→ 首轮验证
→ 审计
→ 发现问题则修复
→ 再验证
→ 标记 IMPLEMENTED
→ 提交 / 推送
→ PR
→ 正式门禁
→ 失败则读取真实日志并继续修复
→ 受控合并
→ 真实 main 复验
→ 永久分支同步
→ 分支卫生
→ delivery-ready
→ assert-delivered
→ 最终汇报
```

任何失败都返回最近一个正确阶段继续：

```text
验证失败 → 定位第一原因 → 修复 → 重跑失败验证 → 扩大关联验证
审计失败 → 修复 → 再验证
PR 门禁失败 → 日志根因 → 修复 → 更新 PR → 全部必要门禁重新通过
main 复验失败 → 任务重新进行 → 来源永久分支修复 → 新闭环
同步/卫生失败 → 查明真实分支与 PR 状态 → 安全修复 → 再验证
```

不得因为失败次数增加而降低验收标准。

## 8. 允许真正停止的条件

只有以下情况允许停止自动推进并请求用户裁决或准确报告阻塞：

- 第 2 节列出的用户保留裁决事项；
- 外部权限、服务或平台故障导致当前工具确实无法继续，且已经穷尽仓库内可执行工作；
- 未知分支历史、未知数据归属或不可逆操作无法安全证明；
- 必要信息在仓库、配置、测试、PR、日志和现有上下文中均不存在。

遇到 `404`、`409`、`422`、权限错误、CI 失败、合并失败、分支不同步等情况时，**错误本身不是停止理由**。必须先核对仓库、Ref、路径、PR、提交、权限、触发条件和真实日志，定位根因后继续处理。

确实无法继续时，最终说明必须包含：

```text
阻塞位置
已验证事实
真实失败原因
已经完成的工作
尚未完成的工作
继续推进缺少的唯一条件
```

禁止包装成“基本完成”。

## 9. 核心架构不变量

### INV-001 插件优先

新增能力优先：

```text
已有能力扩展
→ 已有插件扩展
→ 新插件
→ 最后才修改核心运行循环
```

禁止为局部需求把功能硬编码进核心循环。

### INV-002 能力定义与实现分离

公共能力保持：

```text
能力契约
→ 实现提供方
→ 消费者
```

上层依赖契约，不依赖具体实现内部状态。本地、沙箱或其他执行环境通过替换实现提供方完成，不复制整套上层逻辑。

### INV-003 会话事件和持久状态是运行事实源

模型上下文、恢复、分叉、状态重建、界面投影、查询、统计和审计必须能从权威会话事件与持久状态重建。

禁止让界面状态、缓存或日志成为第二业务真源。

### INV-004 执行环境可替换

文件、Shell、终端、代码执行和开发工具共享上层语义，只替换底层能力提供方。禁止本地、沙箱、远程各维护一套业务逻辑。

### INV-005 工具调用经过统一边界

模型工具不得绕过正式工具执行链。输入验证、权限、执行环境、取消、超时、错误传播、资源释放、结果大小和会话记录必须在代码边界保证，不能靠提示词保证安全。

### INV-006 Host / Client 边界明确

浏览器界面只负责交互和展示。本机文件、进程、环境、凭据、系统资源和服务端状态必须通过正式宿主能力与协议边界提供。

### INV-007 仓库范围保持精简

当前仓库默认不恢复：

```text
.agents/
docs/
website/
examples/
根 assets/
```

除非用户明确要求或任务闭环确实需要，禁止为了测试方便恢复仓库范围外资产。`.github/task-control/` 属于本仓库正式治理闭环，不属于额外产品体系。

## 10. 工程实现原则

任何修改都遵循：

```text
先理解，再修改
先找根因，再写补丁
优先公共正确机制，再处理局部现象
范围尽可能小，但闭环必须完整
```

修改前后都检查完整调用链，包括定义方、注册方、调用方、状态持有方、持久化、界面消费者和测试。

出现以下信号时优先修公共边界：

- 多处复制相同错误处理、重试、Pending、取消或清理逻辑；
- 同一状态存在多个写入者；
- 生命周期没有明确所有者；
- 一个缺陷反复出现在相邻模块；
- 测试依赖固定延时才能稳定；
- 缓存或界面状态能够改变业务真值。

禁止：

- 无边界全仓重写；
- 为一个问题替换整个架构；
- 复制同类补丁；
- 静默重试或宽泛 `catch` 掩盖根因；
- TODO、空实现、固定成功、假数据或临时生产逻辑；
- 为让测试变绿而删除测试、放宽断言或隐藏失败。

## 11. 高风险模块的额外检查

### 公共接口与兼容性

修改公共接口、事件、工具 Schema、会话事件、SDK、ACP 或跨包契约时，必须搜索全部生产和测试调用方，检查序列化、恢复、错误类型、Client/Host 与公开 SDK。非明确要求时保持向后兼容。

### Session 与持久化

必须验证：

```text
写入
→ 读取
→ 重建
→ 继续运行
```

同时覆盖新会话、旧会话恢复、分叉、部分写入、中途失败、取消、压缩前后、重启和界面重建。

### Tool / Shell / Terminal / FS / Runtime / LSP / Sandbox

至少检查输入、权限、路径、工作目录、环境变量、输出、退出码、超时、取消、子进程、资源释放、大输出、空输出、用户可见错误、会话事件和不同执行环境语义一致性。

### Subagent / Jobs / Workflow / Schedule

必须明确创建者、所有者、取消者、回收者、结果归属、父任务退出、失败传播、重复消息、超时和恢复后旧结果处理。禁止无所有者后台任务和旧结果覆盖新状态。

### Web / Client

覆盖空状态、加载、成功、失败、取消、禁用、权限拒绝、长内容、工具执行、会话切换与恢复。界面不得虚构后端能力，缓存不得成为业务真源。

## 12. 测试、环境与真实性

权威环境：

```text
Node.js ^22.19.0 || >=24.0.0
pnpm 11.7.0
```

标准安装：

```bash
corepack enable
pnpm install --frozen-lockfile
```

常用验证：

```bash
pnpm run typecheck
pnpm run build
pnpm run test
pnpm run test:coverage
pnpm run test:e2e
pnpm run test:web
pnpm run test:gui
pnpm run test:snapshot
pnpm run test:web:perf
pnpm run test:web:stress
```

验证按风险分级：局部实现跑相关测试与类型检查；跨包/公共能力增加消费者验证；运行时、会话、工具、沙箱、Host/Client、SDK、构建、依赖或最终验收必须扩大验证范围。

当前精简基线不包含 `examples/` 与 `docs/`，以下测试依赖这些非产品资产：

```text
apps/cli/tests/memory-mcp-configs.spec.ts
packages/shell/tool-pwsh/tests/loader.spec.ts
packages/typert/generator/tests/cordis-catalog.spec.ts
```

不得为了这三项自行恢复整个缺失目录，也不得把它们误判为产品源码故障；除这些已知边界外，任何失败都必须实际调查。

未经真实运行不得说：

- 已通过；
- 已修复；
- 构建正常；
- 测试全绿；
- 没有回归；
- 可以发布。

必须区分代码审查、编译、测试、真实运行和最终主线交付。

## 13. 安全、性能、错误与日志

涉及 Shell、PowerShell、文件写入、代码执行、子进程、终端、沙箱、网络、MCP、外部工具、凭据和工作区边界时，检查参数注入、路径逃逸、权限绕过、沙箱逃逸、凭据/环境泄漏、非预期网络、残留进程和任意文件访问。

安全限制必须由代码保证，提示词不能充当安全边界。

Agent Loop、Session、Context、Tool、Web、Client 热路径修改时检查无界数组、全量重复扫描、大对象重复序列化、热路径同步磁盘、无界并发、监听器泄漏、重复插件注册、重复连接和界面阻塞。禁止无依据性能重构。

错误必须在正确层产生、保留上下文、按契约传播并在合适层转换。禁止静默吞错。

日志只用于诊断，不能改变业务结果；不得记录凭据或无条件记录敏感模型输入；任务结束前清理临时噪音日志。

## 14. 写入、提交与自动化边界

写入前确认：

```text
仓库
永久集成分支
HEAD
任务状态
目标文件
任务范围
```

写入后重新读取真实结果。提交、PR、分支 Ref、合并和工作流操作都必须读取真实结果验证，不得只相信写操作返回“成功”。

提交必须只包含任务范围内的正式修改，禁止混入调试输出、构建产物、缓存、本地环境文件、无关格式化和凭据。提交信息优先使用中文准确描述目的。

自动化可以验证、构建、测试、打包、受控合并、主线验证、永久分支同步、卫生审计和发布状态；自动化不得生成正式业务源码来替代实现，不得篡改测试结果，不得伪造任务状态。

## 15. 完成前强制审计

实现首次完成后必须独立审计：

```text
原始需求是否全部覆盖
完整调用链是否同步
相邻模块是否受影响
接口与错误路径是否完整
取消、超时、恢复与资源释放是否正确
是否出现第二套真源
测试是否真正证明行为
是否有无关修改、调试代码或临时文件
是否新增不必要依赖或安全风险
所有完成声明是否有真实证据
```

发现任务范围内问题直接继续修复并重新验证。禁止把可修问题只写进最终报告留给用户。

## 16. 最终汇报规则

最终汇报前必须完成：

```text
实现
+
验证
+
审计
+
修复后的再验证
+
正式 PR 门禁
+
受控合并
+
真实 main 验证
+
永久分支同步
+
分支卫生
+
delivery-ready
+
repository-state = DELIVERED
```

最终汇报只写真实结果：完成了什么、验证了什么、过程中发现并修复了什么、最终主线和机器状态。

如果仓库尚未达到 `DELIVERED`，不得使用“完成”“闭环”“最终交付”作为结论；继续推进，或在确实无法继续时准确报告唯一阻塞。
