# 安全策略

请通过仓库的 [私有漏洞报告入口](https://github.com/sy220284/555/security/advisories/new) 报告未修复漏洞，不要创建公开 Issue。请包含受影响版本或提交、复现步骤、影响分析、概念验证以及建议缓解措施，并删除真实凭据和个人信息。

维护者会先确认收到报告，再评估严重度、复现、修复与披露窗口。修复发布前请保持细节私密。

`security` 门禁检查工作流 Action 是否固定到完整提交 SHA，并对 `pnpm audit --prod` 使用显式安全债务基线：任何 critical、此前未记录的 advisory ID 或严重度升级都会失败。已记录债务不是永久豁免；依赖更新应逐步移除已解决记录。

第三方声明生成链和 `vendor` 上游溯源记录由各自维护流程负责，不属于本次治理修复范围。
