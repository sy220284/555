# 正式实现推进记录

本文件只记录已经实际进入代码并由自动门禁验证的内容，不把设计、接口、占位或规则级测试冒充完整玩法完成。

## 2026-09-12 第一轮正式推进

### 新增生产主干

- `Packages/com.modernra.rules`：纯C#确定性权威规则内核；
- `Tools/ModernRA.SimRunner`：无画面规则运行器；
- `.github/workflows/simulation-gates.yml`：规则构建与确定性模拟持续集成；
- Unity `Simulation/AI/IntelEW/Robotics/Network/Tests` 改为直接依赖统一规则内核，避免测试逻辑与正式运行逻辑分叉。

### 实跑证据

持续集成环境：Ubuntu 24.04，.NET 8.0.425。

构建结果：

- `0 Warning(s)`；
- `0 Error(s)`。

固定输入：

- 地图：`MAP_GRAY_RANGE`；
- 权威频率：30Hz；
- 活动单位：1000；
- 单次：10000 tick；
- 重复：10次；
- 随机流：显式固定种子。

验证结果：

- 10次最终状态哈希完全一致：`A72CA0F85058C84B`；
- 地图读取3条道路、5个控制区；
- 双方共享方向路径走廊总构建数：2；
- 双方工业结果：`14999.600 / 14999.600`；
- 主采集累计：`20000.000`；
- 第二采集累计：`9999.200`；
- 对称采矿单位损失事件：2；
- 直射武器射击：6424；
- 击毁：636；
- 情报、AI授权、机器人降级、本机权威网络规则门禁通过。

## 门禁状态

| 门禁 | 状态 | 当前证据 | 未完成部分 |
|---|---|---|---|
| G0 数据与仓库 | PASS | 内容验证CI持续通过 | 后续新增数据继续受门禁约束 |
| G1 Unity工程编译 | PENDING | 已锁 `6000.3.24f1`，包/程序集结构已建 | 尚未在真实Unity编辑器环境完整编译并跑EditMode |
| G2 确定性模拟 | PASS（规则内核） | 10000 tick × 10，哈希完全一致 | Unity ECS接入后需重复同级验证 |
| G3 首张地图 | PARTIAL | `MAP_GRAY_RANGE`机器侧车可确定性加载并生成无画面实体模型 | Unity灰盒场景、导航烘焙、实际实体出生未完成 |
| G4 第一经济闭环 | PASS（规则内核） | 基础金属、双采集递减、单位损失、容量非负、双方对称 | Unity采矿实体/处理点表现与ECS批处理未接入 |
| G5 移动与共享导航 | PARTIAL | 1000单位共用2条方向走廊，无逐单位全图路径 | 局部避障、桥路失效、目标CPU P50/P95/P99未测 |
| G6 第一战斗闭环 | PASS（规则内核） | 接近、开火、命中、装甲、生命、击毁均产生实际结果 | 正式弹道/目标选择/ECS批处理和性能未完成 |
| G7 情报与战争迷雾 | PASS（规则内核） | Unknown/Anomaly不下发；四级有效情报精度不同 | Unity传感器探测链和真实客户端快照集成未完成 |
| G8 AI战斗群 | PARTIAL | 所属、区域、权限、禁令、玩家覆盖代次已可执行判权 | 战斗群效用评分、目标/路线选择和实际接管未完成 |
| G9 机器人/EW | PARTIAL | A1—A4断联、黑区、算力不足降级规则已自动验证 | 完整状态矩阵与战场实体行为未接入 |
| G10 网络回环 | PARTIAL | 内容哈希、命令防重放、意图校验、情报过滤、增量快照、插值已通过本机规则回环 | Unity Transport真实传输、丢包/延迟/重连未完成 |
| G11 完整歼灭 | NOT STARTED | 无 | 建设/生产/完整胜负/录像AI对局尚未闭环 |

## 当前生命周期

项目整体仍为 `implementation_bootstrap`。

规则内核已经开始进入可验证实现阶段，但在G1、Unity灰盒、ECS批处理、目标硬件性能和G11完整歼灭全部完成前，不得升级为 `playable`。

## 下一条正式执行链

1. 建立可用Unity 6000.3.24f1自动编译/测试环境，关闭G1；
2. 用 `MAP_GRAY_RANGE` 侧车生成Unity灰盒、控制区、道路、资源点和双方出生实体；
3. 将 `com.modernra.rules` 接入ECS批处理系统，而非在Unity层重写规则；
4. 加入局部空间哈希避障、道路/桥梁版本失效与性能采样；
5. 完成正式传感器探测链、AI战斗群执行、机器人/EW实体行为和Unity Transport回环；
6. 关闭G11第一局完整歼灭后，才进入征服矿产科技与后续模式。
